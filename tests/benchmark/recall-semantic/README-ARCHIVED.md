# ARCHIVED — v1 benchmark retired on 2026-04-23

This directory's `run.sh`, fixture, scenarios were removed in the 2026-04-23 refactor.
Only historical `results/*.md` retained for reference.

Current recall testing: `tests/recall/` (single test scenario set, production metrics via `know-ctl metrics`).
