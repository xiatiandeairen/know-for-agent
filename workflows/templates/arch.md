# {{名称}} 架构设计

## 驱动因素

<!-- 什么问题/约束驱动了这个架构设计。回答"为什么需要架构设计"
  - INCLUDE: 业务规模需求、质量属性要求、技术约束、团队约束
  - EXCLUDE: 具体功能需求（在 PRD 里）、模块内部实现细节（在 tech 里） -->

{{驱动因素}}

## 系统概览

<!-- 系统边界 + 核心职责 + 外部角色。回答"系统长什么样"
  - INCLUDE: 系统边界图、核心子系统、外部依赖方、部署拓扑
  - EXCLUDE: 模块内部类/函数设计、数据结构字段定义 -->

```
{{系统概览图}}
```

{{系统描述}}

## 组件拆分

<!-- 模块划分 + 各自职责 + 边界规则。回答"谁负责什么"
  - INCLUDE: 模块名称、核心职责（一句话）、模块间边界规则
  - EXCLUDE: 模块内部实现（类、函数、文件结构属于 tech） -->

| 组件 | 职责 | 边界规则 |
|------|------|---------|
| {{组件名}} | {{职责}} | {{边界规则}} |

## 依赖与数据流

<!-- 组件依赖方向 + 数据流转路径。回答"怎么协作"
  - INCLUDE: 依赖方向、数据流图、通信方式（同步/异步/事件）
  - EXCLUDE: 具体 API 签名（在 schema 里）、消息格式细节 -->

```
{{依赖/数据流图}}
```

| 依赖方 | 被依赖方 | 类型 | 说明 |
|--------|---------|------|------|
| {{组件A}} | {{组件B}} | {{强/弱}} | {{说明}} |

## 质量属性

<!-- 性能/可用性/安全性的具体指标。回答"要达到什么标准"
  - INCLUDE: 可量化的系统级指标（延迟、吞吐、可用率）
  - EXCLUDE: 功能正确性（在 PRD 验收标准里） -->

| 属性 | 指标 | 目标 |
|------|------|------|
| {{属性}} | {{指标}} | {{目标值}} |

## 约束

<!-- 必须遵守的硬性限制：合规、SLA、技术限制。回答"不能违反什么"
  - INCLUDE: 合规要求、SLA、平台限制、兼容性要求
  - EXCLUDE: 编码规范（在 CLAUDE.md 里） -->

- {{约束条件}}
