# {{模块名}} 架构设计

<!-- 核心问题: 这个模块怎么拆、内部怎么协作？
     定位: 单模块的架构描述（每模块独立文件）
     不属于本文档: 模块内部实现（→ tech）、接口签名（→ schema）、产品需求（→ prd） -->

## 1. 驱动因素

<!-- 什么约束驱动了这个模块的设计。回答"为什么这样设计"
  - INCLUDE: 业务规模需求、质量属性要求、技术约束
  - EXCLUDE: 具体功能需求（→ prd） -->

{{驱动因素}}

## 2. 结构概览

<!-- 模块内部拆分 + 职责。回答"内部长什么样"
  - INCLUDE: 子组件/层次划分、核心职责（一句话）、边界规则
  - EXCLUDE: 函数/类级别设计（→ tech） -->

```
{{结构图}}
```

| 组件 | 职责 | 边界规则 |
|------|------|---------|
| {{组件名}} | {{职责}} | {{边界规则}} |

## 3. 依赖与数据流

<!-- 内部组件间 + 外部模块间的协作方式。回答"怎么协作"
  - INCLUDE: 依赖方向、数据流图、通信方式（同步/异步/事件）
  - EXCLUDE: 具体 API 签名（→ schema） -->

```
{{数据流图}}
```

| 依赖方 | 被依赖方 | 类型 | 说明 |
|--------|---------|------|------|
| {{组件A}} | {{组件B}} | {{强/弱}} | {{说明}} |

## 4. 质量属性

<!-- 本模块的性能/可用性/安全性指标。回答"要达到什么标准"
  - INCLUDE: 可量化指标（延迟、吞吐、可用率）
  - EXCLUDE: 功能正确性（→ prd 验收标准） -->

| 属性 | 指标 | 目标 |
|------|------|------|
| {{属性}} | {{指标}} | {{目标值}} |

## 5. 约束

<!-- 本模块必须遵守的硬性限制。回答"不能违反什么"
  - INCLUDE: 合规要求、SLA、平台限制、兼容性
  - EXCLUDE: 编码规范（→ CLAUDE.md） -->

- {{约束条件}}
